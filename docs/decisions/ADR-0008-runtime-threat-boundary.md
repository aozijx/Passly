# ADR-0008: 定义运行时威胁边界

- 状态：Accepted
- 日期：未记录

## 背景

Android/JVM 无法保证已 Root、可持续读取进程内存的攻击者永远得不到使用中的明文。

## 决策

重点防御静态文件泄露、短时内存暴露、错误日志和越权访问；对完全控制运行时的攻击者明确记录为残余风险。通过缩短会话和尽力覆盖可控数组降低窗口，不做绝对内存擦除承诺。

## 后果

安全声明更可验证，也避免以复杂 Native 隔离制造虚假保证。

## 备选方案

未采用仅为“不可 Dump”宣传而全面迁移 Native 的方案。
