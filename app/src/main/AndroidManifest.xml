<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 前台服务数据同步类型权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:name=".app.PasslyApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/launcher_logo"
        android:roundIcon="@mipmap/launcher_logo_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/local_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/SplashScreenTheme"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.autofill.framework.AutofillFillActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="standard"
            android:noHistory="true"
            android:theme="@style/Theme.Passly.TransparentAuth" />

        <!-- Phase 2: Credential Provider 凭据选择响应 Activity -->
        <activity
            android:name=".feature.autofill.credential.CredentialResponseActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="standard"
            android:noHistory="true"
            android:theme="@style/Theme.Passly.TransparentAuth"
            tools:targetApi="34">
            <intent-filter>
                <action android:name="com.aozijx.passly.ACTION_GET_PASSWORD" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.aozijx.passly.ACTION_GET_PASSKEY" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.aozijx.passly.ACTION_CREDENTIAL_UNLOCK" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.autofill.framework.LegacyAutofillService"
            android:enabled="false"
            android:exported="true"
            android:label="@string/autofill_service_name"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service_config" />
        </service>

        <!-- API 34+ CredentialProviderService，默认关闭，由 Application 动态启用 -->
        <service
            android:name=".service.autofill.credential.ModernCredentialService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_CREDENTIAL_PROVIDER_SERVICE"
            tools:targetApi="34">
            <intent-filter>
                <action android:name="android.service.credentials.CredentialProviderService" />
            </intent-filter>
            <meta-data
                android:name="android.credentials.provider"
                android:resource="@xml/credential_service_config" />
        </service>

        <!-- 支持 Android 13（API 33）及以上系统的“按应用语言偏好”（Per-App Language）功能。-->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="androidx.appcompat.app.AppLocalesMetadataHolderService" />
            </intent-filter>
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
